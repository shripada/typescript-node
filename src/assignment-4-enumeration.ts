/**
 * Enumeration for representing the planets in the solar system
 */

enum Planets {
    Mercury,
    Venus,
    Earth,
    Mars,
    Jupiter,
    Saturn,
    Uranus,
    Neptune,
  }

const planetName = Planets[0];
console.log(planetName);
const planetName2 = Planets[2];
console.log(planetName2);
const planetName1 = Planets[1];
console.log(planetName1);